To correctly run the python files, it is necessary to change the "archivo_csv" line, which is in every file, and the "ruta_excel" line which is in some files,
the ones that create de figures. The change must be done to make python follow the exact path for the network and identifiers in your computer.
